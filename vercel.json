{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@10.24.0 --activate && pnpm install --no-optional --shamefully-hoist --no-frozen-lockfile --ignore-scripts",
  "buildCommand": "pnpm --filter @workspace/med-lamari-portfolio run build",
  "builds": [
    {
      "src": "artifacts/med-lamari-portfolio/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "\\/(.*)", "dest": "/artifacts/med-lamari-portfolio/dist/index.html" }
  ]
}
